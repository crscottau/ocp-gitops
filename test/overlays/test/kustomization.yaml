apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base
#- another-configmap.yaml
- yet-another-configmap.yaml

patches:
- path: configmap.yaml
- patch: |-
    apiVersion: v1
    kind: ConfigMap
    metadata:
      name: random
      namespace: test
    $patch: delete
