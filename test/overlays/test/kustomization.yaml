apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base
#- another-configmap.yaml
- yet-another-configmap.yaml

patches:
- path: configmap.yaml

patchesStrategicMerge:
- |-
  apiVersion: v1
  kind: ConfigMap
  metadata:
    name: random
  $patch: delete
