apiVersion: config.openshift.io/v1
kind: APIServer
metadata:
  name: cluster
spec:
  servingCerts: 
    namedCertificates:
      - names: 
          - api.name.domain
        servingCertificate: 
          name: custom-certs-managed